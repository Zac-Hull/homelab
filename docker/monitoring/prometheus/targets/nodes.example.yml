# Copy this file to nodes.yml and replace placeholder values
# with real runtime targets. Do not commit nodes.yml.

- targets:
    - "node-exporter:9100"
  labels:
    environment: "homelab"
    site: "home"
    host: "monitor01"
    role: "monitoring"
    exporter: "node"

- targets:
    - "<NODE_IP>:9100"
  labels:
    environment: "homelab"
    site: "home"
    host: "documentation01"
    role: "documentation"
    exporter: "node"
